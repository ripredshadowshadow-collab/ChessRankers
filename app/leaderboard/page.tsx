import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function LeaderboardPage(){const supabase=await createClient();const[{data:chess},{data:solo}]=await Promise.all([supabase.from("chess_leaderboard").select("id,username,chess_elo").limit(100),supabase.from("solo_leaderboard").select("id,username,solo_elo").limit(100)]);return <div className="cr-shell"><nav className="cr-nav"><a href="/" className="cr-brand"><span className="cr-mark">♞</span><span>ChessRankers</span></a><div className="cr-links"><a href="/play">Play AI</a><a href="/solo">Solo Chess</a><a href="/pvp">Play a Friend</a><a href="/tester">Move Tester</a><a className="active" href="/leaderboard">Leaderboard</a><a href="/profile">Profile</a></div></nav><main className="chess-page"><div className="match-top"><div><h1>Global Leaderboards</h1><p>Chess Elo and Solo Elo are separate. PvP has no Elo ranking.</p></div></div><div className="cr-feature-grid" style={{gridTemplateColumns:"1fr 1fr"}}><section className="cr-card"><h2>Chess Elo</h2><p>Rated AI games only.</p>{chess?.length?<div className="move-list">{chess.map((p:any,i:number)=><div className="move-row" key={p.id}><span>{i+1}</span><span>{p.username||"Anonymous"}</span><strong>{p.chess_elo}</strong></div>)}</div>:<p className="cr-note">No rated players yet.</p>}</section><section className="cr-card"><h2>Solo Elo</h2><p>Solo Chess runs only.</p>{solo?.length?<div className="move-list">{solo.map((p:any,i:number)=><div className="move-row" key={p.id}><span>{i+1}</span><span>{p.username||"Anonymous"}</span><strong>{p.solo_elo}</strong></div>)}</div>:<p className="cr-note">No Solo players yet.</p>}</section></div><p className="cr-note">PvP results are tracked separately and never affect either rating.</p></main></div>}
